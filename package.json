{
  "name": "oefa-scraper",
  "version": "1.0.0",
  "description": "Scraper para el portal de resoluciones TFA del OEFA",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node ./index.ts",
    "start:prod": "npm run build && node dist/index.ts",
    "dev": "ts-node ./index.ts"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
